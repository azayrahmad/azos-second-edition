{
  "name": "azos-second-edition",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postversion": "node -e \"import fs from 'fs'; const pkg = JSON.parse(fs.readFileSync('./package.json')); fs.writeFileSync('./public/version.json', JSON.stringify({ version: pkg.version }));\""
  },
  "devDependencies": {
    "vite": "^7.0.4",
    "vite-plugin-pwa": "^1.1.0"
  },
  "dependencies": {
    "@playwright/test": "^1.56.1",
    "ani-cursor": "^0.0.5",
    "os-gui": "^0.7.3"
  }
}
